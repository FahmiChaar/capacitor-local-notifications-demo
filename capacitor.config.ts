import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.test.ln',
  appName: 'test-local-notifications',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
